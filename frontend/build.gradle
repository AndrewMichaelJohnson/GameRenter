plugins{
  id "com.github.node-gradle.node" version "7.1.0"
}

version "0.0.1"

node {
  download = true
  version = '18.19.0'
}

tasks.register('build', NpmTask) {
  args = ['run', 'build-java']
}


build.dependsOn(npmInstall)

